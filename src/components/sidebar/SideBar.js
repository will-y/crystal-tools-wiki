import './SideBar.css';
import {Container, Row} from "react-bootstrap";

function SideBar(props) {
    return (
        <Container fluid>
            <Row>

            </Row>
        </Container>
    );
}
